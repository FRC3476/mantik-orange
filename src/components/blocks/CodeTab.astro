---
interface Props {
  label: string;
  value?: string;
}

const { label, value } = Astro.props;
---

<div class="code-tab-content" data-tab-label={label} data-tab-value={value}>
  <p class="code-tab-fallback-label">{label}</p>
  <div class="code-block">
    <slot />
  </div>
</div>
