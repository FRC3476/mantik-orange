---
title: Instance Factory Methods
lessonId: java-dp-factory-method
section: java
group: java-design-patterns
groupLabel: "Design Patterns"
groupOrder: 8
order: 1
difficulty: intermediate
duration: 25 min
description: Using a named method instead of a constructor to create objects, why that reads better, and how it lets you hide which class you actually return.
---

<RulesBox title="Stretch — names you will see in WPILib">

Factory = named creation. Dependency injection = pass what a class needs. Command = wrap work so a scheduler can run it later. These are structures in team code, not extra Java syntax.

</RulesBox>

### What constructors cannot do

A constructor has to be named after its class. That is fine until you need two ways of building the same thing:

```java norun
class Angle {
    private final double radians;

    Angle(double radians) { this.radians = radians; }
    Angle(double degrees) { ... }      // will not compile
}
```

Both take a `double`, so they have the same signature. Java cannot distinguish them, and no amount of [overloading](/java/java-overloading) helps.

A factory method solves it by using a name instead:

```java
class Angle {
    private final double radians;

    private Angle(double radians) {          // private: not called directly
        this.radians = radians;
    }

    static Angle ofRadians(double radians) {
        return new Angle(radians);
    }

    static Angle ofDegrees(double degrees) {
        return new Angle(Math.toRadians(degrees));
    }

    double getRadians() { return radians; }
}

System.out.println("90 deg = " + Angle.ofDegrees(90).getRadians() + " rad");
System.out.println("pi/2 = " + Angle.ofRadians(Math.PI / 2).getRadians() + " rad");
```

Now the call says what it means:

```java
class Angle {
    private final double radians;

    private Angle(double radians) {
        this.radians = radians;
    }

    static Angle ofRadians(double radians) {
        return new Angle(radians);
    }

    static Angle ofDegrees(double degrees) {
        return new Angle(Math.toRadians(degrees));
    }

    double getRadians() { return radians; }
}

Angle a = Angle.ofDegrees(90);
Angle b = Angle.ofRadians(Math.PI / 2);
System.out.println("a=" + a.getRadians() + " b=" + b.getRadians());
```

Compare that with `new Angle(90)`, where a reader has to check the constructor to learn which unit is expected. Unit confusion is a real source of robot bugs, and naming the method removes the ambiguity.

<RulesBox title="Making the constructor private">

Marking the constructor `private` forces everyone through the factory methods. That is what gives you control — you can validate, cache, or change what you return without breaking any caller.

If you leave the constructor public, both routes exist and the guarantees weaken. Make it private unless you have a reason not to.

</RulesBox>

### Static factory versus instance factory

Both are called factory methods, and the difference is what they belong to.

A **static factory** belongs to the class being created, as above. `Angle.ofDegrees(90)`.

An **instance factory method** belongs to some other object, and that object's state influences what gets built:

```java
class Motor {
    final int port;
    boolean inverted;
    double maxOutput = 1.0;

    Motor(int port) { this.port = port; }
    void setInverted(boolean inverted) { this.inverted = inverted; }
    void setMaxOutput(double maxOutput) { this.maxOutput = maxOutput; }
}

class MotorFactory {
    private final boolean inverted;
    private final double maxOutput;

    MotorFactory(boolean inverted, double maxOutput) {
        this.inverted = inverted;
        this.maxOutput = maxOutput;
    }

    Motor create(int port) {
        Motor m = new Motor(port);
        m.setInverted(inverted);
        m.setMaxOutput(maxOutput);
        return m;
    }
}

Motor sample = new MotorFactory(true, 0.8).create(1);
System.out.println("motor " + sample.port + " inverted=" + sample.inverted + " max=" + sample.maxOutput);
```

```java
class Motor {
    final int port;
    boolean inverted;
    double maxOutput = 1.0;

    Motor(int port) { this.port = port; }
    void setInverted(boolean inverted) { this.inverted = inverted; }
    void setMaxOutput(double maxOutput) { this.maxOutput = maxOutput; }
}

class MotorFactory {
    private final boolean inverted;
    private final double maxOutput;

    MotorFactory(boolean inverted, double maxOutput) {
        this.inverted = inverted;
        this.maxOutput = maxOutput;
    }

    Motor create(int port) {
        Motor m = new Motor(port);
        m.setInverted(inverted);
        m.setMaxOutput(maxOutput);
        return m;
    }
}

MotorFactory driveMotors = new MotorFactory(true, 0.8);

Motor left  = driveMotors.create(1);
Motor right = driveMotors.create(2);
System.out.println("left port=" + left.port + " right port=" + right.port);
```

The shared configuration lives in the factory, so you set it once rather than repeating it at every creation site. Add a third motor later and it is automatically configured the same way.

This is the form that pairs with [dependency injection](/java/java-dp-dependency-injection) — you pass a factory to code that needs to create things without deciding how they are configured.

### Returning a subtype

The most powerful consequence: a factory can return something more specific than it promises.

```java
abstract class Drivetrain {
    abstract void drive(double speed, double rotation);
}

class TankDrive extends Drivetrain {
    @Override
    void drive(double speed, double rotation) {
        System.out.println("tank drive speed=" + speed + " rotation=" + rotation);
    }
}

class SwerveDrive extends Drivetrain {
    @Override
    void drive(double speed, double rotation) {
        System.out.println("swerve drive speed=" + speed + " rotation=" + rotation);
    }
}

class DrivetrainFactory {
    static Drivetrain forRobot(String config) {
        if (config.equals("swerve")) {
            return new SwerveDrive();
        }
        return new TankDrive();
    }
}

Drivetrain swerve = DrivetrainFactory.forRobot("swerve");
Drivetrain tank = DrivetrainFactory.forRobot("tank");
swerve.drive(0.5, 0.0);
tank.drive(0.5, 0.0);
```

Callers get a `Drivetrain` and never learn which kind:

```java
abstract class Drivetrain {
    abstract void drive(double speed, double rotation);
}

class TankDrive extends Drivetrain {
    @Override
    void drive(double speed, double rotation) {
        System.out.println("tank drive speed=" + speed + " rotation=" + rotation);
    }
}

class SwerveDrive extends Drivetrain {
    @Override
    void drive(double speed, double rotation) {
        System.out.println("swerve drive speed=" + speed + " rotation=" + rotation);
    }
}

class DrivetrainFactory {
    static Drivetrain forRobot(String config) {
        if (config.equals("swerve")) {
            return new SwerveDrive();
        }
        return new TankDrive();
    }
}

String config = "swerve";
Drivetrain drive = DrivetrainFactory.forRobot(config);
drive.drive(0.5, 0.0);            // whichever implementation, same call
```

[Runtime polymorphism](/java/java-runtime-polymorphism) does the dispatch. The decision about which class to build is made in exactly one place, and every user of `Drivetrain` is unaffected by it.

### Other things factories can do

**Validate before constructing.** A constructor that throws leaves callers with an awkward failure; a factory can return an `Optional` or a sensible fallback.

```java
class Angle {
    private final double radians;

    private Angle(double radians) { this.radians = radians; }

    static Angle ofDegrees(double degrees) {
        return new Angle(Math.toRadians(degrees));
    }

    double getRadians() { return radians; }

    static Optional<Angle> parse(String text) {
        try {
            return Optional.of(ofDegrees(Double.parseDouble(text)));
        } catch (NumberFormatException e) {
            return Optional.empty();
        }
    }
}

Optional<Angle> ok = Angle.parse("90");
Optional<Angle> bad = Angle.parse("nope");
System.out.println(ok.isPresent() ? "parsed " + ok.get().getRadians() : "empty");
System.out.println(bad.isPresent() ? "parsed" : "empty");
```

**Reuse instances.** A constructor must produce a new object every time. A factory may hand back a cached one:

```java
class Angle {
    private final double radians;
    static final Angle ZERO = new Angle(0);

    private Angle(double radians) { this.radians = radians; }

    static Angle ofRadians(double radians) {
        if (radians == 0) return ZERO;         // no new object
        return new Angle(radians);
    }
}

Angle a = Angle.ofRadians(0);
Angle b = Angle.ofRadians(0);
System.out.println(a == b);            // true — same cached instance
System.out.println(a == Angle.ZERO);
```

This is why `Integer.valueOf(5)` is preferred over `new Integer(5)` — the factory caches small values, and the constructor cannot.

**Hide generic clutter.**

```java
// the standard library does exactly this
List<String> names = Arrays.asList("a", "b", "c");
Map<String, Integer> ports = Collections.singletonMap("intake", 3);
System.out.println(names);
System.out.println(ports);
```

<ContentTable
  title="Constructor or factory"
  headers={["Situation", "Use"]}
  rows={[
    ["One obvious way to build it", "Constructor"],
    ["Several ways with the same parameter types", "Static factories with distinct names"],
    ["The units or meaning need naming", "Static factory"],
    ["You may return a subclass", "Static factory"],
    ["You may reuse instances", "Static factory"],
    ["Creation depends on shared configuration", "Instance factory"],
    ["Creation can fail without an exception", "Static factory returning <code>Optional</code>"]
  ]}
/>

### Naming conventions

The standard library is consistent, and following it makes your code predictable:

<ContentTable
  headers={["Name", "Meaning", "Example"]}
  rows={[
    ["<code>of</code>", "Build from the given components", "<code>List.of(...)</code>"],
    ["<code>from</code> / <code>valueOf</code>", "Convert from another type", "<code>Integer.valueOf(\"5\")</code>"],
    ["<code>getInstance</code>", "Return an instance, possibly shared", "<code>Calendar.getInstance()</code>"],
    ["<code>create</code> / <code>newInstance</code>", "Always a fresh object", "&mdash;"],
    ["<code>copyOf</code>", "A copy of the argument", "<code>List.copyOf(...)</code>"]
  ]}
/>

<RulesBox title="Do not overuse it">

A factory for a class with one straightforward constructor is pure overhead. `new Point(3, 4)` is perfectly clear, and wrapping it in `Point.of(3, 4)` adds a layer for nothing.

Add a factory when you have a concrete reason from the table above — naming, subtype choice, validation, or reuse. "Factories are good practice" is not a reason.

</RulesBox>

<RulesBox title="Common mistakes">

- **Leaving the constructor public**, so the factory can be bypassed.
- **Unclear names.** `Angle.make(90)` says nothing that `new Angle(90)` did not.
- **Forgetting `static`** on a static factory, making it impossible to call without an instance.
- **A factory returning the concrete type** when the point was to hide it. Return the abstract type.
- **Mutable cached instances.** Only share objects that cannot be changed after creation, or callers will interfere with each other.

</RulesBox>

<JavaPlayground id="java-dp-factory-method" />

<ExerciseBox title="Practice">

Fill in each starter, click Run, then open Hints if you get stuck.

1. Write an `Angle` class with a private constructor and `ofDegrees` / `ofRadians` factories, plus a `getDegrees()` method. Confirm both routes agree.

```java
class Angle {
    private final double radians;

    private Angle(double radians) {
        this.radians = radians;
    }

    static Angle ofRadians(double radians) {
        return null;
    }

    static Angle ofDegrees(double degrees) {
        return null;
    }

    double getDegrees() {
        return 0;
    }

    double getRadians() {
        return radians;
    }
}

public class Main {
    public static void main(String[] args) {
        Angle fromDegrees = Angle.ofDegrees(90);
        Angle fromRadians = Angle.ofRadians(Math.PI / 2);
        System.out.println(fromDegrees == null ? "null" : fromDegrees.getDegrees());
        System.out.println(fromRadians == null ? "null" : fromRadians.getDegrees());
    }
}
```

2. Add a `ZERO` constant and make `ofRadians(0)` return it. Verify with `==` that you get the same object twice.

```java
class Angle {
    private final double radians;
    static final Angle ZERO = new Angle(0);

    private Angle(double radians) {
        this.radians = radians;
    }

    static Angle ofRadians(double radians) {
        return new Angle(radians);
    }
}

public class Main {
    public static void main(String[] args) {
        Angle a = Angle.ofRadians(0);
        Angle b = Angle.ofRadians(0);
        System.out.println(a == b);
        System.out.println(a == Angle.ZERO);
    }
}
```

3. Write a `Temperature` class with `fromCelsius` and `fromFahrenheit` factories.

```java
class Temperature {
    private final double celsius;

    private Temperature(double celsius) {
        this.celsius = celsius;
    }

    static Temperature fromCelsius(double celsius) {
        return null;
    }

    static Temperature fromFahrenheit(double fahrenheit) {
        return null;
    }

    double getCelsius() {
        return celsius;
    }
}

public class Main {
    public static void main(String[] args) {
        Temperature fromC = Temperature.fromCelsius(0);
        Temperature fromF = Temperature.fromFahrenheit(32);
        System.out.println(fromC == null ? "null" : fromC.getCelsius());
        System.out.println(fromF == null ? "null" : fromF.getCelsius());
    }
}
```

4. Write an abstract `Sensor` with two subclasses, and a static factory choosing between them from a `String`. Confirm the caller only ever sees `Sensor`.

```java
abstract class Sensor {
    abstract String name();

    static Sensor fromType(String type) {
        return null;
    }
}

class Gyro extends Sensor {
    @Override
    String name() {
        return "gyro";
    }

    void calibrate() {
        System.out.println("calibrating");
    }
}

class Distance extends Sensor {
    @Override
    String name() {
        return "distance";
    }
}

public class Main {
    public static void main(String[] args) {
        Sensor sensor = Sensor.fromType("gyro");
        System.out.println(sensor == null ? "null" : sensor.name());
        // Uncomment and click Run — it should not compile, because the type is Sensor:
        // sensor.calibrate();
    }
}
```

5. Write a `MotorFactory` holding shared configuration and creating configured `Motor` objects from a port number.

```java
class Motor {
    int port;
    boolean inverted;
    double maxOutput = 1.0;

    Motor(int port) {
        this.port = port;
    }

    void setInverted(boolean inverted) {
        this.inverted = inverted;
    }

    void setMaxOutput(double maxOutput) {
        this.maxOutput = maxOutput;
    }
}

class MotorFactory {
    private boolean inverted;
    private double maxOutput;

    MotorFactory(boolean inverted, double maxOutput) {
        this.inverted = inverted;
        this.maxOutput = maxOutput;
    }

    Motor create(int port) {
        return null;
    }
}

public class Main {
    public static void main(String[] args) {
        MotorFactory factory = new MotorFactory(true, 0.8);
        Motor left = factory.create(1);
        Motor right = factory.create(2);
        System.out.println(left == null ? "null" : "left port=" + left.port + " inverted=" + left.inverted + " max=" + left.maxOutput);
        System.out.println(right == null ? "null" : "right port=" + right.port + " inverted=" + right.inverted + " max=" + right.maxOutput);
    }
}
```


<details>
<summary>Hints</summary>

1. Store radians internally; convert on the way in and out.
2. A `static final` field, returned when the argument is 0.
3. Same shape as `Angle`. Pick one internal unit and convert at the boundary.
4. Return the abstract type from the factory. Try calling a subclass-only method on the result and note the compile error — that is the encapsulation working.
5. Constructor takes the configuration; `create(int port)` applies it.

</details>

</ExerciseBox>

<LinkGrid
  title="Next"
  section="java"
  links={[
    {"label":"Dependency Injection","id":"java-dp-dependency-injection"},
    {"label":"The Command Pattern","id":"java-dp-command"}
  ]}
/>

<LinkGrid
  title="Related"
  section="java"
  links={[
    {"label":"Runtime Polymorphism","id":"java-runtime-polymorphism"},
    {"label":"Method and Constructor Overloading","id":"java-overloading"}
  ]}
/>
